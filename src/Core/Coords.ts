export type CartesianCoords = {
  readonly x: number,
  readonly y: number,
};

export type CubeCoords = {
  readonly x: number,
  readonly y: number,
  readonly z: number,
};

export type RenderedCoords = {
  readonly x: number,
  readonly y: number,
};
